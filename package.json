{
  "name": "nestjs-prisma-pagination",
  "version": "1.0.6",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "./dist"
  ],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.17.5",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "build": "rm -rf dist && tsc && esbuild --bundle src/index.ts --outdir=dist --minify --platform=node --packages=external"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/asaje379/nestjs-prisma-pagination.git"
  }
}
